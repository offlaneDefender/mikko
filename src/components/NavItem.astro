---
interface Props {
    text: string;
    href: string;
}
const {text, href} = Astro.props
---
<a href={href} class="hover:bg-ctp-surface hover:border-ctp-peach border border-transparent flex items-center justify-center px-1 rounded">
    <p class="text-ctp-text">{text}</p>
</a>